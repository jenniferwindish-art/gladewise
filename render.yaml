services:
  - type: web
    name: bladewise
    runtime: node
    plan: free
    buildCommand: "echo no build step needed"
    startCommand: "node --experimental-sqlite server.mjs"
    envVars:
      - key: NODE_VERSION
        value: "22.11.0"
